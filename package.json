{
  "name": "linkedin-job-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "scripts": {
    "clean": "rimraf .dist/*",
    "build": "pnpm run clean && npx tsc ./src/index.ts --outDir .dist/",
    "start": "pnpm run build && node .dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.49.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
